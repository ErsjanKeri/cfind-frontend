/**
 * Database Client - STUBBED (Prisma Removed)
 *
 * All database operations now go through FastAPI backend
 * This file kept for backward compatibility only
 * Will be deleted after all components are migrated
 */

// Prisma removed - export stub to prevent import errors
export const prisma = null as any;

// Note: Any code trying to use prisma will get runtime errors
// This is intentional - forces migration to backend API
